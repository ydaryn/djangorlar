w4 #django
base py - django set only
git worktree
git pull =git fetch+ git merge
init global import, должен быть в каждой папке
assert?

MVC model view control - монолит, general
model- данные
view- логика, хтмл шаблон
controller- между ними (url)
MTV model template control- в джанго
Приложение- независимый кусок (Новости в ВСП)

py manage.py startapp news
.env
apps-> python ../manage.py startapp tasks
delete test
view function or class. *Args (tuple) **kwargs(dict)
from typing import any
*args:tuple[any,…], **kwargs: dict[str,any]

#django modules
from django.shortcuts import render

template in app folder
джинджа? 
добавляем в юрлс
зарегать в бэйс
регаем папку apps или в apps.py-> apps.tasks
докстринг

django documentation->the model layer, view…
